/**
 * @hidefate/core-events —— 事件层：把「哪些象在场」翻成一件具体的、有时间的事。
 *
 * 象意字典给的是取象范畴（「竞争、创业、独当一面承压」），那是古法的语言。
 * 用户要的是「八月工作上可能被裁」——中间缺的就是这一层。
 *
 * 事件名、结算标准、提前量全部事先写死在模板表里，判定由 allOf/anyOf/noneOf
 * 三个声明式条件完成。**不含任何模型生成**，AI 只被允许解释，不得新造事件。
 *
 * `noneOf` 表达古法的制化 —— 七杀有食神制则不作凶论。
 * 这是事件层比象意层准的关键：象意只说「七杀能主官非」，
 * 事件层还要看它被制住了没有。
 */

export * from './types.js';
export * from './tokens.js';
export * from './templates.js';
export * from './match.js';
