/**
 * 「AI 是可选的」常驻声明。
 *
 * 文案直接取自 README「设计上的几条硬规则」第 1 条 ——
 * *确定性优先，LLM 绝不发明数字*：所有星位、宫位、组合、概率都由
 * `packages/core-*` 的纯函数算出，模型只被允许把这些已有结论写成通顺中文。
 *
 * 放在 lib 而不是组件里，是为了让这条规则可被测试直接断言（测试跑在 node
 * 环境，不渲染 DOM），也让 AI 设置页与 AI 对话页共用同一份措辞 ——
 * 两处若各写各的，迟早会漂移成两种说法。
 */

export const AI_OPTIONAL_NOTICE = {
  title: '确定性优先，AI 绝不发明数字',
  /** 主句：AI 做什么、不做什么。 */
  body:
    '所有星位、宫位、组合、概率都由本机的确定性引擎算出，全程离线。' +
    'AI 只被允许把这些已经算好的结论写成通顺中文 —— ' +
    '它不参与任何计算，也无权修改、重算或新增任何数字。',
  /** 结论句：不配置 AI 也完全够用。 */
  tail:
    '因此不配置 AI 完全不影响使用：三派排盘、概率预测、化解清单、离线作答与报告一应俱全，' +
    '差别只在文字较为格式化。',
  /** 冲突时以谁为准。 */
  precedence: '若模型给出的星位或数字与九宫盘不一致，一律以九宫盘为准。',
} as const;

/** 单行版本，用于空间紧张处（如对话页顶部的一行提示）。 */
export const AI_OPTIONAL_NOTICE_ONE_LINE =
  'AI 只负责把已算好的结论写成通顺中文，不参与任何计算；不配置 AI 也能完整使用本 App。';
